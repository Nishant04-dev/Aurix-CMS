// Update this page (the content is just a fallback if you fail to update the page)

// IMPORTANT: Fully REPLACE this with your own code
const PlaceholderIndex = () => {
  // PLACEHOLDER: Replace this entire return statement with the user's app.
  // The inline background color is intentionally not part of the design system.
  return (
    <div className="flex min-h-screen items-center justify-center bg-background">
       <div className="flex flex-col items-center gap-4 animate-in fade-in zoom-in duration-1000">
          <div className="h-16 w-16 rounded-2xl bg-primary flex items-center justify-center text-primary-foreground text-3xl font-bold shadow-xl shadow-primary/20">A</div>
          <h1 className="text-2xl font-bold tracking-tight text-foreground">Aurix Development</h1>
          <p className="text-muted-foreground text-sm font-medium uppercase tracking-[0.3em]">Client Portal</p>
       </div>
    </div>
  );
};

const Index = PlaceholderIndex;

export default Index;
